import { GrdFilterPipe } from './grdfilter.pipe';

describe('GrdfilterPipe', () => { 
  
  it('create an instance', () => {
    let pipe = new GrdFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
